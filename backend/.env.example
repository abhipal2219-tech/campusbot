# Required environment variables for the Render backend
# Copy this file to .env for local development — NEVER commit .env to git

# Your Groq API key (get one free at https://console.groq.com)
GROQ_API_KEY=

# Port — Render sets this automatically. Default: 10000
PORT=10000
